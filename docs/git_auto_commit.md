# Git Auto-Commit Tool

## 概述

`git_auto_commit` 工具讓 AI 能自動分析程式碼變更、為每個檔案撰寫說明、執行 git commit，並詢問使用者是否 push。

## 相關檔案

| 檔案 | 類型 | 說明 |
|------|------|------|
| `tools/git_commit_tool.go` | 新增 | GitAutoCommitTool 實作 |
| `tools/init.go` | 修改 | 註冊工具 |

## 支援操作

| Action | 說明 |
|--------|------|
| `commit` | 分析 `git diff`，為每個檔案生成變更說明，執行 `git add -A` + `git commit` |
| `push` | 執行 `git push` |
| `rollback` | `git reset HEAD~1` 撤銷最後一次 commit |

## 參數

| 參數 | 必填 | 說明 |
|------|------|------|
| `action` | ✅ | commit / push / rollback |
| `message` | ❌ | 自訂 commit 訊息（留空則自動生成） |

## 自動生成 Commit Message 格式

```
chore: 新增 2 檔, 修改 3 檔 (2026-02-13 14:08)

變更檔案:
- tools/git_commit_tool.go: [新增] 新增檔案
- internal/core/definition.go: [修改] 修改內容
- tools/old_file.go: [刪除] 刪除檔案
```

## 檔案狀態圖示

| 圖示 | 狀態 |
|------|------|
| 🆕 | 新增檔案 |
| ✏️ | 修改內容 |
| 🗑️ | 刪除檔案 |
| 📝 | 重新命名 |

## AI 互動流程

```
使用者: 幫我 commit 這些變更
   ↓
AI 呼叫 git_auto_commit(action: "commit")
   ↓
工具：git status → 解析變更 → git add -A → git commit
   ↓
AI: 已完成 commit (abc1234)，變更如下：
     🆕 tools/git_commit_tool.go — 新增檔案
     ✏️ internal/core/definition.go — 修改內容
     是否要 push？
   ↓
使用者: 好，push
   ↓
AI 呼叫 git_auto_commit(action: "push")
   ↓
AI: ✅ 已推送到遠端
```

如果使用者不回應或拒絕，commit 保留在本地。
若需撤銷，AI 可呼叫 `rollback` 操作。
